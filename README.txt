This is a Python bot that automatically applies to jobs on zip-recruiter.
The basic function is to click the "one-click-apply" buttons,
but the more advanced function is to use parameters as answers in more complicated applications